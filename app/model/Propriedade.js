export default class Propriedade {
    constructor (nome, cidade, estado, usuarioId, imagem) {
        this.nome = nome;
        this.cidade = cidade;
        this.estado = estado;
        this.usuarioId = usuarioId;
        this.imagem = imagem;
    }
}